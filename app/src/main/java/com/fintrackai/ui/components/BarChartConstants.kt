package com.fintrackai.ui.components

object BarChartConstants {
    /** Each month column: label + bar + month name; wider than bar so amounts are not clipped. */
    const val MONTHLY_CELL_WIDTH_DP = 64
    /** Colored bar width inside the cell (centered). */
    const val MONTHLY_BAR_TRACK_WIDTH_DP = 26
    const val CHART_BAR_HEIGHT_DP = 120
}
