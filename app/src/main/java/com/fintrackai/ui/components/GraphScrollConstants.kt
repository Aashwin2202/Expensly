package com.fintrackai.ui.components

object GraphScrollConstants {
    /** Extra items on each side of the visible window used to scale bar heights (smoother at edges). */
    const val VIEWPORT_NEIGHBOR_PAD = 1
}
